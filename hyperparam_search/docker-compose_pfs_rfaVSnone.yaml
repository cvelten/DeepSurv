version: "3.8"

services:
  hp_search:
    build:
      context: ./
      dockerfile: Dockerfile_pfs_rfaVSnone
    volumes:
      - ../deepsurv/:/deepsurv
      - ./logs:/shared/logs
      - ../experiments/data/:/shared/data